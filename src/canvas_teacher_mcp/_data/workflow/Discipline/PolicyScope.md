# Policy Scope

Read a rule only from `CourseGlobalWorkflow/`. Found anywhere else, delete it or replace
it with a one-line pointer here.

Outside links here. It never restates.

A rule says how something is done and does not age. Anything that goes stale is resolved
from its source at read time.

See also: `Writing.md`.
