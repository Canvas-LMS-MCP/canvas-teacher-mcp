# Writing a rule

Write a sentence only if the rule is weaker without it.

Write what to do: `X goes through Y`, not `do not do X`.

Write examples with placeholders — `<course>`, `<school>`, `<org>`, `<student-login>`. A real
name in an example is a person or an institution travelling wherever the document travels, and
these documents ship.
