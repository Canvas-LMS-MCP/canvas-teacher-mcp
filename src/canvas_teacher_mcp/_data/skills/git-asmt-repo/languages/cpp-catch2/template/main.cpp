// L2 cpp-catch2 TEMPLATE — driver. The graded logic lives in main.hpp (tests.cpp includes it);
// this file only exercises it so the Compile and Run graders have something to build and run.
#include "main.hpp"

int main()
{
    cout << "twice(21) = " << twice(21) << endl;
    return 0;
}
