#ifndef MAIN_HPP
#define MAIN_HPP

// L2 cpp-catch2 TEMPLATE — the graded unit is a FUNCTION, so the functions live here and
// tests.cpp includes this header. Per assignment: replace the prototypes and bodies below.
//
// Starter rule (L1 Part C): the shipped Starter keeps every signature and the `// TODO` line,
// with the body stubbed (non-void returns a neutral value) so the repo still COMPILES.

#include <iostream>
#include <string>
using namespace std;

int twice(int n);

int twice(int n)
{
    // TODO: replace with this assignment's function
    return n * 2;
}

#endif
