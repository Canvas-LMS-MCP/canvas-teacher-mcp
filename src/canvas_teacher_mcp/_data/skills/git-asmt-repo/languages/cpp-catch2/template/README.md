# <CODE> — <assignment title>

Write your code in `main.hpp` (the functions) and use `main.cpp` as the driver.
Full instructions are on Canvas.

## Compile (read any errors it prints)
```
g++ -std=c++17 -Wall -Wextra main.cpp -o main
```

## Run
```
./main
```

## Test (the same checks the autograder runs)
```
make test
```
`make test` builds `tests.cpp` (the Catch2 harness — do not edit it) and runs every case.

Submit by committing and pushing; the green check on GitHub means the autograder passed.
