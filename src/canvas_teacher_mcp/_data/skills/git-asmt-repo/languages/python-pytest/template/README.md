# <CODE> — <assignment title>

Write your code in `main.py`. Full instructions are on Canvas.

## Run (read any errors it prints)
```
python3 main.py < data/data.txt
```

## Test (the same checks the autograder runs)
```
pytest -rP -m T1
```
Run `T1`, `T2`, `T3`, `T4` the same way; each group is worth 20 points.

Submit by committing and pushing; the green check on GitHub means the autograder passed.
