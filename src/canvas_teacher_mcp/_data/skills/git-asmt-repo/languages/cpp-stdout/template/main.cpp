// L2 cpp-stdout TEMPLATE — chapters BEFORE functions are taught: the graded unit is the whole
// program's OUTPUT. Read input from stdin (the Run grader feeds data/dataN.txt), print the result.
//
// Starter rule (L1 Part C): keep the structure and the `// TODO` comments, strip the answer.
#include <iostream>
using namespace std;

int main()
{
    // TODO: read this assignment's input from stdin
    int a, b;
    cin >> a >> b;

    // TODO: compute and print what the instruction asks for
    cout << "sum = " << a + b << endl;

    return 0;
}
